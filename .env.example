# Postgres local (Postgres.app v17, puerto 1702 — mismo cluster que usa b2b).
# La app es dueña de sus propias bases rueda_negocios_*.
DB_HOST=localhost
DB_PORT=1702
DB_NAME=rueda_negocios_development
DB_NAME_TEST=rueda_negocios_test
DB_USER=fecego
DB_PASSWORD=fecego

# Sync-down: origen del dataset (rueda-api) y rueda a descargar.
RUEDA_API_URL=http://localhost:4568
RUEDA_ID=3
